services:
  timeback-server:
    build:
      context: .
      dockerfile: server/Dockerfile
    environment:
      TIMEBACK_DB_URL: jdbc:h2:mem:timeback;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
      TIMEBACK_SERVER_PORT: 8080
    ports:
      - "8080:8080"
    restart: "no"
